export const DEFAULT_ICONIFY_API_BASE = 'https://api.iconify.design'

/**
 * A small, curated pool of well-known Iconify collections to pick a random
 * default from when no iconLibrary restriction is set - picking from
 * Iconify's full ~236-collection catalog risked landing on something
 * obscure or low quality as the "surprise" default.
 */
export const DEFAULT_BROWSABLE_PREFIXES = [
  'tabler',
  'carbon',
  'fa',
  'fluent',
  'ion',
  'material-symbols',
  'ant-design',
]
